<h1><%= @collection.name %></h1>

<%= paginate @collections %>

<% @collection.posts.each do |post| %>
	<p><%= post.body %> <%= link_to 'Edit', edit_post_path(post) %> <%= link_to 'Delete', post, data: { confirm: 'Are you sure you want to delete this post?' } %></p>
<% end %>